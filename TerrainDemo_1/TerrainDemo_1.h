#pragma once

#include "DemoApp.h"

class TerrainDemo_1 : public DemoApp
{
public:
	TerrainDemo_1(int windowWidth, int windowHeight, const char* windowTitle) :
		DemoApp(windowWidth, windowHeight, windowTitle)
	{
	}

private:
};